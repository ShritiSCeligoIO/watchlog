properties:
  OPEN_LIBRARY_BASE_URL:
    is_sensitive: false
    devops_configured: false
    description: Base URL for Open Library requests
  TMDB_API_KEY:
    is_sensitive: true
    devops_configured: true
    description: API key used for TMDB movie search
