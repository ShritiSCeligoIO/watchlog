properties:
  OPEN_LIBRARY_BASE_URL:
    is_sensitive: false
    devops_configured: false
    description: Base URL for Open Library requests
  TMDB_API_KEY:
    is_sensitive: true
    devops_configured: true
    description: API key used for TMDB movie search
  REMOTE_WATCHLOG_URL:
    is_sensitive: false
    devops_configured: false
    description: Origin serving the WatchLog Module Federation remote
  GATEWAY_PORT:
    is_sensitive: false
    devops_configured: false
    description: Port used by the production remote gateway
  GATEWAY_ALLOWED_ORIGINS:
    is_sensitive: false
    devops_configured: false
    description: Comma-separated origins allowed to load the production remote
