# Copy to host/.env to override the committed defaults. Nothing here is
# required — the host builds and runs without this file.

# Load the remote from somewhere other than its dev server. Use
# http://localhost:4001 for the Express gateway serving a production build.
# REMOTE_WATCHLOG_URL=http://localhost:3001
