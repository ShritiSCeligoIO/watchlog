# Safe committed defaults; real secrets belong in the ignored `.env`.
TMDB_API_KEY=
OPEN_LIBRARY_BASE_URL=https://openlibrary.org

# Production remote gateway defaults.
GATEWAY_PORT=4001
GATEWAY_ALLOWED_ORIGINS=http://localhost:3000
